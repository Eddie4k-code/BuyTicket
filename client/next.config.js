// next.config.js
module.exports = {

    // Enable TypeScript support
    typescript: {
        // Enable type-checking during development builds
        // This can be optionally disabled to improve build speed
        // but it is recommended to keep it on for better code quality
        check: true,
    },
}